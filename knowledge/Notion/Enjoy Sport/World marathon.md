  

→ Khi tạo order thì sẽ tạo phía BE, dùng FE để flush data từng chút từng chút

  

→ Khi xoá nó sẽ call xuống BE để xoá mà ko xoá được là nó hiện vậy luôn, ko báo lỗi gì hết

  

- Hay fetch lại data để show lên ở FE
- fetch lại thông tin giải chạy
- Lấy danh sách câu hỏi custom : cái này phải fetch khác nhau này