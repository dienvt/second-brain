## PATH
The PATH is an environment variable that specifies a set of directories where executable programs are located. When you type a command in the terminal, the shell looks through these directories, in the order specified by the PATH variable, to find the executable corresponding to that command.
