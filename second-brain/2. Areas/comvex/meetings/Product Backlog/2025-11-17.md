Keep the idle time shorter? It because the connection go away? Is it?

the context cancel is using when we call cancel

BFF get client information
Admin Site Document in the New Admin Web App
## Admin Page, User.

 For getting number of user, whe have to jump to the client then calculate the whole use table and the owner.
Whe already have the API
For each Ternant switch to the tenant database then query the owner and count the total user.
use three endpoints and 
This include the 
// account
able 
How to implement the tables: learning curse

## [DGM2-24966] List administrator

Update the List rpc: Query from auth-api user who belong to the Admin Group
Have to add two new filter in the list user in authAPI:
* GroupID
* Text
The table don't need has Company name

Adminstrator don't belong to account
Don't need any action for now.
Don't Need all company filter

  

## [DGM2-24970] Create an adminstrator 
#### Related Update UserPassword, user have to update the password before use
Auth API : Add new API for create the admin user: create user then assigned admin role? we will update the already exists API for this
#### Do we need to force user change password?
No we don't
#### What if the user's already exists ???
Admin web app create a admin user -> the BFF call to the auth APi if the user already exist errorm the front end will show popup to add the user to admin group
