# Diagram 

```mermaid

erDiagram

Application {

string Id PK

string Name

string Code

string Description

uint AccountId FK "nullable"

Status Status "enabled/disabled"

datetime CreatedAt

datetime UpdatedAt

}

  

Client {

string Id PK

string Name

string Description

string Code

Platform Platform "web"

string Secret "hashed"

string[] Scopes

string ApplicationId FK

Status Status "enabled/disabled/revoked"

uint AccountId FK "nullable"

uint UserId FK "nullable"

datetime CreatedAt

datetime UpdatedAt

}

  

User {

string Id PK

string Username

string Password "hashed"

uint64 AccountId FK "nullable"

string AccountCode "nullable"

uint64 BackendAppAccountUserId FK "nullable"

string BackendAppAccountUserUid FK "nullable"

datetime ActivatedAt

datetime CreatedAt

datetime UpdatedAt

}

  

AccessToken {

uint64 AccountId FK

string Id PK

string ClientId FK

string AccessToken

duration ExpiresIn

string RefreshToken

string SignatureSecret

Type TokenType "Bearer"

GrantType GrantType "password/client_credentials/refresh"

string UserId FK "nullable"

}

  

RefreshToken {

string Id PK

uint64 AccountId FK

string ClientId FK

string UserId FK

string[] Scopes

string AccessTokenId FK

duration ExpiresIn

GrantType GrantType

string SignatureSecret

string Token

}

  

Scope {

string Id PK

string Name

string Description

datetime CreatedAt

datetime UpdatedAt

}

  

Token {

string AccessToken

duration ExpiresIn

string RefreshToken

duration RefreshTokenExpiresIn

string TokenType

}

  

MapClaims {

uint64 AccountId "deprecated"

string AccountCode "deprecated"

uint64 InstigatorId "deprecated"

string UserId

string Scope

string GrantType

string ClientId

string[] UserRoles "new RBAC"

string[] UserGroups "new RBAC"

string[] EffectiveScopes "new RBAC"

jwtRegisteredClaims Claims

}

  

Role {

string Id PK

string Name

string Description

uint64 AccountId FK "nullable"

Status Status "active/inactive"

Policy[] Policies "embedded"

datetime CreatedAt

datetime UpdatedAt

}

  

Policy {

string Id PK

string Name

string Description

string Version "2012-10-17"

Statement[] Statements

}

  

Statement {

string Sid

Effect Effect "allow/deny"

string[] Actions

string[] Resources

map Conditions "optional"

}

  

Group {

string Id PK

string Name

string Description

uint64 AccountId FK "nullable"

Status Status "active/inactive"

string[] Roles "role IDs"

string[] Users "user IDs"

datetime CreatedAt

datetime UpdatedAt

}

  

UserRole {

string Id PK

string UserId FK

string RoleId FK

uint64 AccountId FK "nullable"

datetime AssignedAt

datetime ExpiresAt "nullable"

}

  

GroupRole {

string Id PK

string GroupId FK

string RoleId FK

uint64 AccountId FK "nullable"

datetime AssignedAt

datetime ExpiresAt "nullable"

}

  

%% Relationships

Application ||--o{ Client : "has many"

Client ||--o{ AccessToken : "generates"

Client ||--o{ RefreshToken : "generates"

User ||--o{ AccessToken : "authorizes"

User ||--o{ RefreshToken : "authorizes"

AccessToken ||--o| RefreshToken : "linked to"

Scope ||--o{ Client : "assigned to"

AccessToken ||--o| Token : "converted to"

AccessToken ||--o| MapClaims : "contains"

  

%% RBAC Relationships

User ||--o{ UserRole : "assigned roles"

Role ||--o{ UserRole : "assigned to users"

User ||--o{ Group : "member of groups"

Group ||--o{ GroupRole : "has roles"

Role ||--o{ GroupRole : "assigned to groups"

Role ||--o{ Policy : "contains policies"

Policy ||--o{ Statement : "contains statements"

```

## Key Relationships

  

1. **Application → Client**: One-to-Many

  

- An application can have multiple clients

- Each client belongs to one application

  

2. **Client → AccessToken**: One-to-Many

  

- A client can generate multiple access tokens

- Each access token is generated by one client

  

3. **Client → RefreshToken**: One-to-Many

  

- A client can generate multiple refresh tokens

- Each refresh token is generated by one client

  

4. **User → AccessToken**: One-to-Many

  

- A user can authorize multiple access tokens

- Each access token can be authorized by one user (nullable)

  

5. **User → RefreshToken**: One-to-Many

  

- A user can authorize multiple refresh tokens

- Each refresh token is authorized by one user

  

6. **AccessToken → RefreshToken**: One-to-One

  

- Each access token can be linked to one refresh token

- Each refresh token is linked to one access token

  

7. **Scope → Client**: Many-to-Many

- Scopes are assigned to clients as string arrays

- Multiple clients can have the same scopes

  

## Business Logic Notes

  

- **AccountId**: Used for multi-tenancy, can be null for system-level entities

- **Grant Types**:

- `password`: User credentials grant (allows refresh tokens)

- `client_credentials`: Client-only grant (no refresh tokens)

- `refresh`: Token refresh grant

- **Token Types**: Currently only supports `Bearer` tokens

- **Platforms**: Currently only supports `web` platform

- **Status**: Applications and clients have status fields for lifecycle management

- **Secrets**: Client secrets are stored as Argon2 hashes

- **Deprecated Fields**: MapClaims contains deprecated fields for backward compatibility