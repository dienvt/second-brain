đang có một con consumer -> cần consume message từ một con sqs ?

  

Missing Auth when call workman

Using service token

Create service token của workman

Authorization.go của workman dùng service context

  

participant connected/disconnected